server:
  port: 8080
spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        - id: catalogue-service-route
          uri: lb://catalogue-service
          predicates:
            - Path=/api/v1/series/**
          filters:
            - StripPrefix=2
